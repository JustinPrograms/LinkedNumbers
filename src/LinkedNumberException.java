/**
 * LinkedNumberException is a custom RuntimeException used to indicate exceptional
 * conditions related to linked numbers.
 */
public class LinkedNumberException extends RuntimeException {

    /*
     * @param msg the detail message (which is saved for later retrieval by the getMessage() method)
     */
    public LinkedNumberException(String msg) {
        super(msg);
    }
}
